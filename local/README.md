# Local Workspace

This directory is for local-only utilities and scratch files that should not be part of the Git-tracked reproducible path.

## Conventions
- Put one-off exploratory or migration scripts under `local/adhoc_scripts/`.
- Keep reproducible analysis entry points under `scripts/`.
- Do not rely on files under `local/` from notebooks or repo-facing docs.
